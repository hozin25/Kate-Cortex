{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd frontend && ELECTRON_SKIP_BINARY_DOWNLOAD=1 npx --yes pnpm@10.30.0 install --frozen-lockfile",
  "buildCommand": "cd frontend && npx --yes pnpm@10.30.0 run build:web",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "includeFiles": "backend/src/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
